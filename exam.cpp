#include <iostream>

using namespace std;

int main()
{
    int choice;
    cin >> choice;
    if (choice == 1){cout << "Hello World";}
    else if (choice == 2) {long long x, y; cin >> x; cin >> y; cout << x * y;}
    return 0;
}